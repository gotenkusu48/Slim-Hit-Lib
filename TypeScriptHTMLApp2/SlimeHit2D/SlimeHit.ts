module SlimeHit {
    class SlimeHit {
        HitCheckPoint2Point(x: Vecter.Vector[], y: Vecter.Vector[]): void {
            var ary = new Array;
            for (var i = 0; i < y.length; i++) {
                //Xの配列のどれにアクセスするか
                var x_x:number = x[i].x;//XのX座標
                var x_y: number = x[i].y;//XのY座標
                var y_x: number = y[i].x;//YのX座標
                var y_x: number = y[i].y;//YのY座標

                var result = new Array;
                if (x_x == y_x & x_y == y_x) {
                }

            }
        }
    }
}