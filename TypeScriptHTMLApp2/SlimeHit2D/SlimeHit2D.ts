
module SlimeHit2D {
    class SlimeHit2D {
        HitCheck(x: Vecter.Vector2D[], y: Vecter.Vector2D[]): void{
            var ary = new Array;
            for(var i = 0; i < y.length;i++){
                //Xの配列のどれにアクセスするか
                var x_x = x[i].x;
                var x_x = x[i].x2;
                var x_y = x[i].y;
                var x_y = x[i].y2;
                

        }
        }
    }
}