
module SlimeHit2D {
    class SlimeHit2D {
        HitCheck(x: Vecter.Vector[], y: Vecter.Vector[]): void{
            var ary = new Array;
            for(var i = 0; i < y.length;i++){
                //Xの配列のどれにアクセスするか
                var x_x = x[i].x;
                var x_y = x[i].y;
                

        }
        }
    }
}